# Supply ticketing (autom8-support)
# Base URL of the standalone support service — no trailing slash
VITE_SUPPORT_API_URL=http://localhost:8090

# Dedicated Munafe Supply portal (separate auth — shown to lob_type=supply owners)
VITE_SUPPLY_PORTAL_URL=https://app.autom8.works/supply/login
